import store from './store'
import { router, server } from './api'

class {
  onCreate() {
    console.log(router)
    server.use(router)
  }
  onMount() {
    store.subscribe(() => {
      this.forceUpdate()
    })
  }
}

Game(store.getState())
